// UI module version (owned by the frontend). The About panel merges this with
// the backend module versions from /api/version. Keep in sync with
// version.py's UI_VERSION when you ship a frontend change.
export const UI_VERSION = "1.20.0";
